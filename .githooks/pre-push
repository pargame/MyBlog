#!/usr/bin/env bash
# Quick pre-push: run a fast build to catch obvious issues
set -e

echo "Running quick build (vite) before push..."
# Use npm ci to ensure node_modules consistent; use a short build step
npm run check --silent || {
  echo "pre-push check failed. Aborting push."
  exit 1
}

echo "pre-push checks passed."
